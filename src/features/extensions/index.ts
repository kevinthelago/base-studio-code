// Extensions feature — public API (#1309): MCP servers + hooks. The app imports UI from here;
// the pure domain (resolvers, catalogs) is imported from @/features/extensions/lib/* directly.
export { McpScreen, HooksView } from "./McpScreen";
