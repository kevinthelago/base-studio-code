{
  "name": "Deploy",
  "glyph": "⎈",
  "gate": "shipping + dependencies locked",
  "deps": [
    "repos"
  ],
  "gateRule": {
    "require": [
      {
        "signal": "deploymentDefined",
        "target": true,
        "label": "define target, environments, pipeline, secrets & release"
      },
      {
        "signal": "dependenciesDefined",
        "target": 1,
        "label": "lock at least one dependency in the manifest"
      }
    ]
  },
  "output": "issues",
  "blurb": "How each service ships — target, environments, pipeline, secrets, release — and the libraries it depends on.",
  "prompt": "Define how this project SHIPS and what it DEPENDS ON — the two halves of getting it running.\n\n**1. Shipping**, per service (one per linked repo): the deploy TARGET — the hosting `platform` +\n`workload` (static / serverless / container / long-running) and the git `host` the repo lives on\n(github / gitlab / bitbucket / selfhosted). For a CONTAINER workload also set its image `registry`,\n`orchestrator` (k8s / swarm / nomad) and `replicas` (1 / 3 / 5 / auto) — these fill the pane's\ncontainerization & orchestration card. Then the ENVIRONMENT ladder (dev → staging → prod, each\nmapped to a branch), the CI/CD PIPELINE (build → test → deploy, with the gating stage), CONFIG +\nSECRETS per environment (names only — values live in the host vault), the RELEASE strategy\n(recreate / rolling / blue-green / canary) + rollback policy, and HEALTH checks / observability.\nRecord it with `bsc-plan deploy set` (config JSON on stdin) — each service takes `id`, `repo`,\n`path`, `stack`, `host`, `platform`, `workload`, `region`, `build`/`output` (or `runtime` +\n`registry`/`orchestrator`/`replicas` for containers). That fills the Deploy pane; `bsc-plan deploy\nget` shows it.\n\n**2. Dependencies** — lock the libraries every repo needs ONCE, here, so the parallel fleet never\neach adds and redefines them in their own worktrees (which collide at integration). Record the\nmanifest with `bsc-plan deps set` (manifest JSON on stdin) — like `deploy set`, it stores into\nplan.db (NOT a loose file):\n```\nbsc-plan deps set <<'JSON'\n{\n  \"registries\": { \"internal\": { \"url\": \"https://npm.internal/\", \"scope\": \"@acme\", \"auth\": \"INTERNAL_NPM_TOKEN\" } },\n  \"dependencies\": [\n    { \"repo\": \"owner/app\", \"ecosystem\": \"npm\",   \"name\": \"zod\",       \"version\": \"^3.23\", \"why\": \"schema validation\" },\n    { \"repo\": \"owner/app\", \"ecosystem\": \"npm\",   \"name\": \"@acme/ui\",  \"version\": \"^2\",   \"source\": \"internal\", \"why\": \"design system\" },\n    { \"repo\": \"owner/api\", \"ecosystem\": \"cargo\", \"name\": \"serde\",     \"version\": \"1\",      \"why\": \"(de)serialization\" }\n  ]\n}\nJSON\n```\nPer dependency: `repo` (`owner/repo`; omit ⇒ every repo of that ecosystem), `ecosystem` (`npm` or\n`cargo`), `name`, `version` (omit ⇒ latest), `dev` (test-only), `why`, and `source` — the registry\nKEY when it does NOT come from the public default (npm registry / crates.io). Define each non-public\nregistry once in `registries` (its `url`, npm `scope`, and `auth` = the SECRET NAME for its token),\nand **record that same `auth` secret in the deploy secrets above** so the pipeline can fetch it.\n`deps set` replaces the WHOLE manifest, so include every dependency in one call; `bsc-plan deps get`\nshows the stored manifest. These become each repo's real `package.json` / `Cargo.toml` (+ `.npmrc` /\n`.cargo/config.toml` for private sources) at publish, and are inlined into every worker as the\nlocked, authoritative set. These shipping + dependency decisions become deployment ISSUES owned by a\n`deploy` stream at publish.\n\nGate: shipping defined (target per service, ≥2 environments, staged pipeline, secrets, release\nstrategy) AND at least one dependency locked via `bsc-plan deps set`."
}
