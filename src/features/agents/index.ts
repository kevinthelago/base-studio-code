// Agents (Permissions) feature — public API (#1309): the per-session least-privilege roles +
// permission profiles surface. The screen; the agent profiles / audit / enforcement logic is in
// @/features/agents/lib/*. (Agent profile STATE lives in the core store slice, not here.)
export { AgentsScreen } from "./screens/AgentsScreen";
