// GitHub feature — public API (#1309): the GitHub screen (connect flow, summary, repos, projects).
// The GitHub API client + helpers live in @/features/github/lib/* (cross-cutting; imported directly).
export { GitHubScreen } from "./screens/GitHubScreen";
