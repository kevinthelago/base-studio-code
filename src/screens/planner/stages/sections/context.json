{
  "name": "Context",
  "glyph": "◆",
  "gate": "every required topic confirmed",
  "deps": [],
  "requires": ["goal", "scope", "stack", "architecture", "users"],
  "gateRule": {
    "require": [
      {
        "signal": "requiredContextConfirmed",
        "target": true,
        "label": "write & confirm every required context topic"
      },
      {
        "signal": "topicsResolved",
        "of": "topicsTotal",
        "weight": 0,
        "label": "confirm the required topics"
      },
      {
        "signal": "hasPlanGaps",
        "target": false,
        "weight": 0,
        "label": "resolve TODO/FIXME markers in the written sections"
      }
    ]
  },
  "blurb": "Context — the fundamental files that give Claude a clear picture (goal, scope, stack, architecture, users + applicable dimensions).",
  "prompt": "Establish the project's context — one topic at a time — as a markdown file per topic at\n`context/<topic>.md` (the canonical key is the file stem). The REQUIRED set is dynamic and\nproject-specific: the baseline `goal, scope, stack, architecture, users` is seeded for you, but\nshape it for THIS project with `bsc-plan context require <topic>` / `bsc-plan context unrequire\n<topic>` as the picture clarifies — e.g. a CLI tool unrequires `users`/`ux`; a data platform\nrequires `schema`; a realtime API requires `api`. `bsc-plan context list` shows the manifest.\n\nFor each topic: propose it, interrogate it with the user, then WRITE the file. Cover non-required\ndimensions only where they genuinely apply (canonical keys: ux, schema, api, auth, security,\ntesting, …); record the rest in `context/_skipped.md`. Finish each file fully — no TODO / TBD /\nFIXME / XXX / TKTK markers (the gate blocks on them; ordinary \"...\" or \"placeholder\" is fine).\n\nYou NEVER confirm a topic yourself — only the USER confirms (the gate item). Write and require;\nthe user reviews and approves.\n\nGate: every required topic is written AND confirmed by the user.",
  "substeps": [
    {
      "key": "goal",
      "label": "Goal",
      "gate": "context/goal.md written and confirmed",
      "prompt": "Establish the goal: the single outcome this project must achieve, and who it's for. Propose\nit, interrogate it with the user, then write `context/goal.md`. Stop and let the user confirm before moving on."
    },
    {
      "key": "scope",
      "label": "Scope",
      "gate": "context/scope.md written and confirmed",
      "prompt": "Define what's in scope and — just as important — what's explicitly out of scope. Write\n`context/scope.md`, then let the user confirm."
    },
    {
      "key": "stack",
      "label": "Stack",
      "gate": "context/stack.md written and confirmed",
      "prompt": "Decide the technology stack — languages, frameworks, datastores — and the reasoning behind\neach choice. Write it to the file named exactly `context/stack.md` (not \"tech stack.md\" or a\ntitled variant — the gate looks for the `stack` stem), then let the user confirm."
    },
    {
      "key": "architecture",
      "label": "Architecture",
      "gate": "context/architecture.md written and confirmed",
      "prompt": "Describe the high-level architecture: the major components and how they interact. Write\n`context/architecture.md`, then let the user confirm."
    },
    {
      "key": "users",
      "label": "Users",
      "gate": "context/users.md written and confirmed",
      "prompt": "Describe who uses this — the user roles/personas and what each needs from the app. Write\n`context/users.md`, then let the user confirm. (Unrequire this with `bsc-plan context unrequire users` if the project genuinely has no distinct users, e.g. a pure CLI or library.)"
    },
    {
      "key": "dimensions",
      "label": "Applicable dimensions",
      "loop": "topics",
      "prompt": "Now cover any remaining dimension that genuinely applies — one at a time — using the canonical\nkey as the file stem (`context/ux.md`, `context/schema.md`, `context/api.md`, `context/auth.md`,\n`context/security.md`, `context/testing.md`, …); `bsc-plan context require <topic>` any you want\nto gate on. Record every dimension you don't document in `context/_skipped.md`. Don't create files\nfor tangential topics."
    }
  ]
}
