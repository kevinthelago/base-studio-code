[package]
name = "bsc-graph"
version = "0.1.0"
edition = "2021"
description = "The Algorithms knowledge graph (#2760/#2761) — the curated concept ontology, queryable from `bsc graph`."

[dependencies]
bsc-util = { path = "../bsc-util" }
# The shared FS-confinement gate (#3475) — `require_within_repo_root`, the CLI twin of the
# `bsc-confine` hook, so a harvest cannot read outside a confined session's root.
bsc-cli-util = { path = "../bsc-cli-util" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tree-sitter = "0.26"
tree-sitter-typescript = "0.23"
tree-sitter-rust = "0.24"
