// #4126 fixture — the colocated test harvest pairs `sample.ts` with THIS file.
describe("sample algorithms (#4126)", () => {
  it("is carried onto every candidate harvested from sample.ts", () => {
    expect(true).toBe(true);
  });
});
