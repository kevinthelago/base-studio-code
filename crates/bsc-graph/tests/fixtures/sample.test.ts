// #4125b fixture — the colocated test harvest pairs `sample.ts` with THIS file.
describe("sample algorithms (#4125b)", () => {
  it("is carried onto every candidate harvested from sample.ts", () => {
    expect(true).toBe(true);
  });
});
