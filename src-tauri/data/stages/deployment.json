{
  "directive": "**Deployment** — ONE stage, two parts. (1) **Link** the repositories this project spans: emit `<repo_link owner/repo>` for each (clones into the hub + records the link in plan.db; `bsc plan repo add owner/repo`, `bsc plan repo list`). (2) **Ship**: define how each service DEPLOYS, recorded in plan.db — `bsc plan deploy set` (config JSON: `services`, ONE per repo — each a self-contained deployable unit carrying its OWN `platform`+`workload`, ≥2 `environments`, a staged `pipeline`, `secrets`, and a `release.strategy`). The gate needs BOTH: ≥1 repo linked AND every repo's service deploy-ready. `bsc plan deploy get` shows the stored config. (Dependencies are locked in the **Streams** stage now, #1429 — not here.)",
  "name": "Deployment",
  "glyph": "⎈",
  "gate": "shipping defined",
  "deps": [
    "repos"
  ],
  "gateRule": {
    "require": [
      {
        "signal": "deploymentDefined",
        "target": true,
        "label": "define target, environments, pipeline, secrets & release"
      }
    ]
  },
  "output": "issues",
  "blurb": "How each service ships — target, environments, pipeline, secrets, release — and the libraries it depends on.",
  "prompt": "Define how this project SHIPS and what it DEPENDS ON — the two halves of getting it running.\n\n**1. Shipping**, per service (one per linked repo). Each service ships in one of two MODES — pick the honest one:\n\n- `mode: \"cloud\"` (default) — a hosted service. Set the deploy TARGET: the hosting `platform` +\n`workload` (static / serverless / container / long-running) and the git `host` (github / gitlab /\nbitbucket / selfhosted). For a CONTAINER workload also set its image `registry`, `orchestrator`\n(k8s / swarm / nomad) and `replicas` (1 / 3 / 5 / auto).\n- `mode: \"local\"` — a library or a build-and-run-here app (a CLI, a desktop app, a local server). NO\ncloud host. Set `localKind`:\n  - `\"library\"` → `publishRegistry` (npm / crates.io / PyPI / internal), `packageName`, a `build`\ncmd, and `publishTrigger` (on-tag / manual). Its pipeline ends in `publish`, not `deploy`.\n  - `\"application\"` → `buildTargets` (OS/arch list or \"desktop installer\"), `build` cmd, `artifact`,\n`runCmd`. Optionally `portForward: { enabled, port, method }` to expose a locally-running app\nremotely — `method` is cloudflared / ngrok / tailscale / LAN (default cloudflared). Its pipeline\nends in `package`.\n\nThen, for every service, the ENVIRONMENT ladder (dev → staging → prod, each mapped to a branch), the\nCI/CD PIPELINE (build → test → deploy|publish|package, with the gating `test` stage), CONFIG +\nSECRETS per environment (names only — values live in the host vault), the RELEASE strategy\n(recreate / rolling / blue-green / canary) + rollback policy, and HEALTH checks / observability.\nRecord it with `bsc plan deploy set` (config JSON on stdin) — each service takes `id`, `repo`,\n`path`, `stack`, `mode`, plus the mode-appropriate fields above. That fills the Deploy pane;\n`bsc plan deploy get` shows it.\n\nThese shipping decisions become deployment ISSUES owned by a `deploy` stream at publish.\n\nGate: shipping defined — every repo's service has a target, ≥2 environments, a staged pipeline, secrets, and a release strategy. (Dependencies are locked in the Streams stage now, #1429.)",
  "icon": "conveyor_belt",
  "hue": 30
}
