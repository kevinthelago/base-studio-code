{
  "$schema": "../node_modules/@tauri-apps/cli/schema/acl/capability.schema.json",
  "identifier": "default",
  "description": "Default capability — grants core Tauri APIs to the main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
